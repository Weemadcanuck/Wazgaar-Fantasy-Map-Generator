# Obsidian Fork 1.154.8

Small interface cleanup before the Archival Fork split.

- Moved Map export into Tools → Export, alongside Obsidian export. Removed the duplicate bottom button.
- Shortened Original notes backup to Notes backup so the label fits. These are map notes preserved during migration, not release notes.
- Updated the guided tour for the new export location.

Retains the fullscreen rendering fix from 1.154.6. Map formats and export behavior are unchanged.

Download and run **azgaar-obsidian-fork-1.154.8-win-x64.exe**. Close the app before installing. Checksums are in `SHA256SUMS.txt`.
